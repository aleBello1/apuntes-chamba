////////////////////////INSTRUMENTAR SPRING BOOT CON OPENTELEMETRY (LOCAL)//////////////////////

Instrumentar una aplicación significa: Agregarle la capacidad de medir y reportar lo que está haciendo 
internamente.
Si un servicio está instrumentado:
- Mide cuánto tardó.
- Mide cuántas veces se llamó.
- Mide si fue error 500.
- Guarda un trace con todos los pasos internos.
- Puede reportar métricas, logs y trazas.

OpenTelemetry es un estándar open source para instrumentar aplicaciones.
Puede enviar logs, métricas y traces.

Grafana tempo: Es la base de datos donde se guardan los requests de tu aplicación (las trazas).
Una traza es El recorrido completo de una petición dentro del sistema.
Span: Una operación individual dentro del trace

El flujo ideal de herramientas debe ser el siguiente:

App (Combinado con el agente OpenTelemetry)
        ↓
Collector (receiver → processor → exporter)
        ↓
Tempo (almacena trazas)
        ↓
Grafana (visualiza)



IMPLEMENTACION DEL AGENTE OPENTELEMETRY AL PROYECTO

Dirigirse al repositorio oficial y descargar la versión mas reciente de este agente (deberá 
ser un archivo .jar): 
https://github.com/open-telemetry/opentelemetry-java-instrumentation?tab=readme-ov-file

El archivo se mueve a la raíz del proyecto y se ejecuta el siguiente comando.

OTLP = OpenTelemetry Protocol
Es el protocolo estándar de OpenTelemetry para enviar: traces, métricas y logs.

java -javaagent:opentelemetry-javaagent.jar \
-Dotel.service.name=mi-app \
-Dotel.traces.exporter=otlp \
-Dotel.metrics.exporter=none \
-Dotel.logs.exporter=none \
-Dotel.exporter.otlp.endpoint=http://localhost:4317 \
-Dotel.exporter.otlp.protocol=grpc \
-jar ./target/app-1.0.0.jar -> Levanta la aplicación pero antes carga un agente que la 
	instrumenta automáticamente.
	- -javaagent:nombreAgente.jar -> Antes de arrancar mi aplicación, carga este agente.
		Este agente intercepta llamadas HTTP, HDBC, redis, clientes HTTP.
	- -Dotel.service.name=mi-app -> Define el nombre lógico del servicio.
		En grafana aparecerá este nombre.
	- -Dotel.traces.exporter=otlp -> Para que la app exporte trazas con el mecanismo de 
		exportación llamado OTPL.
	- -Dotel.metrics.exporter=none -> Desactiva exportación de métricas.
		Porque Tempo solo recibe traces.
		Si no lo desactivas, el agente intentará enviarlas y puede generar errores.
	- -Dotel.logs.exporter=none -> Desactiva exportación de logs.
		Mismo motivo.
		Si quisieras logs necesitarías algo como: Loki o algo similar.
	- -Dotel.exporter.otlp.endpoint=http://localhost:4317 -> Para que envié todo lo que se
		haya configurado (trazas, métricas y/o logs) a determinada ruta.
		Aquí escucha tempo.
	- -Dotel.exporter.otlp.protocol=grpc -> Indica como se envían los datos que se hayan
		configurado (trazas, métricas y/o logs) con el exporter otlp.
	- -jar ./target/app-1.0.0.jar -> Indicar donde vive el jar de la aplicación.
		Evidentemtne se debió haber generado el .jar antes de usar este comando.

Para confirmar que todo este bien, los logs de la aplicación se deben de ver limpios.


IMPLEMENTACION Y CONFIGURACION DEL COLLECTOR
 
Es un proxy inteligente de telemetría.
Collector puede enviar cada señal (trazas, logs y/o métricas) a un diferente destino: Traces → 
Tempo, Métricas → Prometheus y Logs → Loki.
Tiene un sampling inteligente pe: Enviar solo 20% de traces, Enviar 100% si son errores ó Enviar
100% si tardan más de 1s.

El Collector es opcional y actúa como intermediario entre las aplicaciones instrumentadas con 
OpenTelemetry y los backends de observabilidad (grafana). No reemplaza al agente OpenTelemetry; 
lo complementa agregando capacidades como procesamiento, enrutamiento, buffering y sampling 
avanzado.

Es importante usar collector en prod porque combate varios problemas:
1. si hay varias apps enviando sus datos directamente a tempo no se podrá limitar el trafico, ni 
filtrar spans y ni se podrá reducir el volumen, con collector si se puede lograr todos eso.
2. Si tempo se cae, las trazas se pierden pero si esta collector algunas trazas se puede guardar
un rato en collector.
3. mejora eficiencia porque collector envia bloques de spans.

Se vuelve a crear el contenedor de tempo pero ahora solo exponiendo el puerto principal (el puerto
3200), los demás puertos se quitan. Primero se hace esto porque el collector se conectara a 
tempo.

MSYS_NO_PATHCONV=1 docker run -d \
  --name tempo \
  -p 3200:3200 \
  --network red \
  -v volTempo:/traces \
  -v volTempoWal:/etc/tempo/tempo_wal \
  -v /c/users/alejandro/desktop/MAPFRE/ws-mapfre/tempo.yaml:/etc/tempo/tempo.yaml \
  ubuntu/tempo:2.7.1-24.04_stable -> Para crear un contenedor con la imagen de tempo y metiendo
	nuestro archivo de configuración.
	En los logs se debe de ver un mensaje que diga: 'msg="Tempo started"' significando que 
	todo esta ok.

Se debe crear un archivo llamado otel-collector-config.yaml

Si el Collector fuera una fábrica:
- Receivers = puerta de entrada
- Processors = área de transformación
- Exporters = camiones que llevan mercancía

receivers: -> Configuraciones para recibir datos (traces, metrics o logs) de otro sistema.
  otlp_grpc:
    protocols:
      grpc: 
        endpoint: 0.0.0.0:4317 -> Crea una puerta de entrada para recibir datos otlp 
		con el protocolo grpc.
		Por convencion este protocolo usa el puerto es 4317 y se debe especificar
		que escuche en todas las interfaces de red.
		Debe escuchar en todas las interfaces de red para permitir conexión de 
		otros contenedores u otros hosts.
		Mas adelante cuando se levante la app con el agente, se indicara que se 
		envíen los datos aquí. ***

processors: -> Los processors modifican o procesan datos antes de enviarlos.
	Los processors se ejecutan en orden (de izq a der, según se hayan metido en el 
	arreglo que se le pasa a los processors).
	Configuraciones para esta opción:
	Configuraciones para loki y tempo ------------------------------------
	- batch: -> Agrupa muchas trazas/logs/métricas y después las envía juntos.
		Configuraciones para esta opción:
		En pruebas los valores recomendados son: 1, 64 y 128 respectivamente y para 
		producción son: 5, 512 y 1024 respectivamente.
		- timeout: ns -> Si no se llena el batch, el collector espera máximo n
			segundos antes de enviarlo.
		- send_batch_size: m -> Cuando se acumulan m spans, se envían 
			inmediatamente.
		- send_batch_max_size: z -> Proteje contra batches gigantes.
			Si llegan mas de m spans al mismo tiempo, lo máximo que se enviara
			serán z spans.
	- probabilistic_sampler:
    	     sampling_percentage: n -> Guarda solo el n% de las trazas aleatoriamente.
	- resource: -> Para agregar enriquecimiento a los datos. Significa agregar información 
		extra a las trazas, métricas o logs antes de enviarlos.
		Muchas veces la aplicación no incluye toda la información útil, entonces el 
		collector la agrega automáticamente.
    	     attributes:
      		- - key: nombreClave
       		  value: valor
        	  action: tipoAction -> Crea una clave y valor que será agregado a los datos.
			Tipos de accion:
			- insert -> Agrega el atributo solo si no existe.
			- upsert -> Inserta o reemplaza.
			- delete -> Elimina un atributo.
	- memory_limiter: -> Este processor protege al collector para que no se quede sin 
		memoria RAM y no se caiga, porque cuando tempo se cae el collector mantiene
		los datos en memoria, si no se configura esto el collector usara toda la
		memoria ram.
		Configuraciones para esta opción:
		- limit_percentage: 70 -> Si el proceso usa más del 70% de la memoria disponible
			empieza a rechazar datos
		- spike_limit_percentage: 25 -> si la memoria aumenta demasiado rápido
			limita cuánto puede crecer en ese pico.
  		- check_interval: 5s -> Cada n segundos el collector revisa cuanta memoria 
			esta usando
	Configuraciones para loki ------------------------------------
	- attributes:
    	    actions:
            - key: loki.attribute.labels
              action: upsert
              value: nombreCampo1, nombreCampo2, nombreCampoN -> Tomando campos existentes, los
		convierte en campos labels.

exporters: -> Configuraciones para enviar los datos a otro sistema.
	Configuraciones para esta opción: 
	Para mandar datos a tempo ---------------------------------------
	- otlp_grpc: -> Crea un objeto exporter.. En teoría este nombre es indiferente 
		porque es como si fuera el nombre de un objeto donde lo importante es 
		su contenido, sin embargo, la documentación recomienda este nombre.
		- endpoint: nombreContenedorTempo:4317 -> Para que se envíen datos al 
			servicio llamado tempo el cual escucha el protocolo grpc en el 
			puerto 4317.
    	    	- tls:
		     insecure: true -> No verifiques certificados TLS.
			Permite conexión sin validación segura.
		- retry_on_failure: -> Define como el collector reintentara enviar datos cuando 
			la herramienta que recibiría los datos no los recibe.
			Configuraciones para esta opción: 
			En pruebas los valores recomendados son: 1, 5 y 30 respectivamente y para 
			producción son: 5, 30 y 300 respectivamente.
			- enabled: true -> Activia los reintentos.
			- initial_interval: ns -> El primer retry se hace después de n segundos.
			- max_interval: ys -> Es el tiempo máximo entre retries.
			- max_elapsed_time: zs -> Tiempo máximo total intentando enviar los datos.
		- sending_queue: -> Guarda temporalmente los datos antes de enviarlos
			En pruebas los valores recomendados son: 2 y 1000 respectivamente y 
			para producción son: 4 y 1000 respectivamente.
			La cola guarda batches, no spans individuales.
			- enabled: true -> Activa la cola.
			- num_consumers: 4 -> Número de workers enviando datos en paralelo.
			- queue_size: 10000 -> Máximo número de batches en la cola.
				Si se llena los datos nuevos se descartan.
			- storage: file_storage -> Para que persista los datos en caso de que no
				se puedan enviar.
	- debug:
		- verbosity: detailed -> sirve para imprimir en los logs del collector 
			los datos de los pipelines.
			Básicamente para debuggear o verificar que sí están llegando 	
			traces/metrics/logs.
	Para mandar datos a loki ---------------------------------------
	- loki:
		Configuraciones para esta opción:
    	    	- endpoint: http://nombreContenedorLoki:3100/loki/api/v1/push -> Para mandar los 
			logs a loki.
		- default_labels_enabled: -> Controla qué labels automáticas añade Loki a TODOS 
			los logs.
			- exporter: false -> NO añade la label "exporter" a los logs.
				Si estuviera en true, los logs dirían de donde viene los logs 
				(de loki)
			- job: true -> Añade el label que representa el nombre de proyecto.
	Para mandar datos a promethues ---------------------------------------
	- prometheus:
		Configuraciones para esta opción:
		- endpoint: "0.0.0.0:9464" -> Indica el puerto donde quedaran expuestas las métricas
			para que promethues pueda scrapear.
			Nuevamente es en todas las interfaces para exporner esto afuera del contenedor.
		- send_timestamps: true -> Incluye el timestamp original de la métrica.
			Sin esto Prometheus pone su propio timestamp
		- metric_expiration: 180m -> Tiempo que una métrica vive sin actualizarse.
		- enable_open_metrics: true -> Usa formato moderno (OpenMetrics)


extensions: -> Para agregar funcionalidades auxiliares al collector
	Configuraciones para esta opción:
	- health_check: -> Crea el endpoint de salud para el collector.
		Por defecto el endpoint se encuentra en localhost:13133.
		- endpoint: 0.0.0.0:13133 -> Para modificar donde se encuentra el endpoint de 
			healthcheck. Al escuchar en todas las interfaces se puede usar este
			servicio afuera del contendor.
	- file_storage:
    	    directory: /var/lib/otelcol -> habilita almacenamiento persistente dentro del 	
		collector.
		Esta ruta deberá de coincidir con la ruta del volumen persistente para el
		contenedor. *

service:
 - telemetry:
    metrics:
      address: 0.0.0.0:8888 -> Permite monitorear el collector con Prometheus o Grafana.
	Evidentemtne si se configura esto, se debe configurar prometheus para que consuma
	este servicio.
 - extensions: [health_check, file_storage] -> habilita un endpoint de salud del collector.
	Esto es usado por otros contenedores para ver el estado de saludo del colletor.
 - pipelines: -> Define los pipelines, el cual se encargan de recibir, procesar y exportar
	datos.
	Cada telemetria tiene su propio pipeline.
	El collector no procesa datos si no están en un pipeline.
	Configuraciones para esta opción:
	Configuración para tempo ---------------------------------------
	- traces: -> Indica el pipeline de las trazas.
		Configuraciones para esta opción:
		- receivers: [nombreReceiver1, nombreReceiver2, nombreReceiverN] -> Para
			indicar que receiver's de los que se configuraron en la etiqueta
			receiver llegaran las trazas al collector.
			No importa que dentro de la etiqueta exporters se tenga uno con 
			el mismo nombre porque no se definen los nombres de manera
			global, cada sección es independiente, entonces solo buscara 
			los receivers definidos dentro de la etiqueta recervers.
			Lo común seria colocarle solo otlp para indicar que recibirá 
			traces de tipo OTLP. 
      		- processors: [nombreProccesor1, nombreProccesor2, nombreProccesorN] -> Se le pasa 
			todos los processors que queremos que tenga esta señal.
			Estos proccesor se debieron definir anteriormente dentro de la etiqueta
			proccessors. ***
			Si se tiene muchos processors el orden correcto debería de ser:
			resource, attributes, sampling y batch.
      		- exporters: [nombreExporter1, nombreExporter2, nombreExporterN] -> Para
			indicar que exporter's de los que se configuraron en la etiqueta
			exporters se enviaran las trazas del collector hacia afuera.
			Se envían al exporter OTLP (Tempo).
	Configuración para loki ---------------------------------------
	- logs:
	    receivers: [otlp]
    	    processors: [memory_limiter, batch]
            exporters: [loki]
	Configuración para prometheus ---------------------------------------
	- logs:
	    receivers: [otlp]
    	    processors: [memory_limiter, batch]
            exporters: [prometheus]

La imagen del OpenTelemetry Collector ejecuta el proceso con un usuario no root por razones de seguridad.
Cuando Docker crea un volumen de persistencia, este se crea con propietario root:root y permisos 755.
Debido a esto, el usuario interno del collector no puede escribir en el volumen, lo que provoca errores 
de permission denied. Para solucionarlo, primero se crea el volumen y después se modifican sus permisos 
desde un contenedor temporal.

docker volume create volCollector

Este comando monta el volumen en un contenedor temporal basado en Alpine Linux y cambia los permisos 
para permitir lectura, escritura y ejecución a todos los usuarios.

MSYS_NO_PATHCONV=1 docker run --rm -it -v volCollector:/data alpine chmod -R 777 /data

MSYS_NO_PATHCONV=1 docker run -d \
  --name collectorDefinitivo \
  -p 4317:4317 \
  -v volCollector:/var/lib/otelcol \
  --network red \
  -v /c/users/alejandro/desktop/MAPFRE/ws-mapfre/otel-collector-config.yaml:/etc/otelcol-contrib/config.yaml \
  otel/opentelemetry-collector-contrib:0.102.1 -> Crear un contenedor para el collector.
	- -p 4317:4317 -> Este es el puerto principal del collector y es para recibir con el
		tipo de exportación OTLP datos por el protocolo gRPC.
	- -p 8888:8888 -> Puerto opcional de abrir. Se usa si se quiere que prometheus
		monitoree el collector y habilitarlo en la configuración.
	- /etc/otelcol-contrib/config.yaml -> Archivo de configuración que carga el contendor
		al iniciar.

La única manera que se encontró para visualizalizar el contenido de archivo de configuración fue
mover desde el contendor el archivo a nuestra maquina local. Esto se hizo para comprobar que se
toma nuestro archivo y no el que viene por default.
docker cp collectorDefinitivo:/etc/otelcol-contrib/config.yaml ./config.yaml

Logs del collector -------------------------------------

Datos que podemos encontrar en los logs del contenedor que almacena a collector:

"otelcol.component.id": "nombreExporter" -> Contiene el nombre de un exporter.
	Los exporters se definen en el arhicvo otel-collector-config.yml, adicionalmente vienen
	otros exporters que existen por defecto.
	Exporters que existen por defecto:
	- debug -> NO envía datos a ningún lado externo.
		Solo imprime en consola.

"otelcol.component.kind": "tipoComponente" -> Contiene el tipo de componente.
	- receiver -> Recibe datos
	- processor -> Modifica / filtra
	- exporter -> Envía a otro sistema
	- extensión -> Son funciones operativas (debug, healthcheck, pprof, etc.)
		- healthcheck -> Sirve para que Kubernetes o Docker puedan verificar si el Collector 
			está vivo.
		- zpages -> Sirve para ver internamente qué está pasando en el Collector:
			Qué receivers están activos
			Cuántos spans están fluyendo
			Qué exporters están enviando datos

"otelcol.component.id": "otlp",
"otelcol.component.kind": "receiver",
"endpoint": "[::]:4317" -> Ver algo asi indica que esta perfectamente preparado el collector para
	recibir datos otlp por el puerto 4317.

"otelcol.signal": "traces" -> Para confirmar que realmente esta recibiendo trazas.

Si al ver al final de los logs se ven mensajes como (usando Docker logs collector --tail 100):
Starting GRPC server
Everything is ready. Begin running and processing data.

java -javaagent:opentelemetry-javaagent.jar \
-Dotel.service.name=mi-app \
-Dotel.traces.exporter=otlp \
-Dotel.metrics.exporter=none \
-Dotel.logs.exporter=none \
-Dotel.exporter.otlp.endpoint=http://localhost:4317 \
-Dotel.exporter.otlp.protocol=grpc \
-jar ./target/app-1.0.0.jar -> Se ejecuta el mismo comando para levantar la app con openTelemetry 
	solo que se debe considerar que ahora la app se conectara al contenedor que almacena al 
	collector y no a tempo.

En grafana ajustar el nombre del contenedor de tempo (en caso de que se haya usado otro nombre de
contenedor).


IMPLMENTACION DEL COLLECTOR MAS PROMETHEUS

Si se tienen varios proyectos mandando métricas al collector, el collector sabe de que proyecto
vienen porque al levantar el proyecto con la bandera: -Dotel.service.name=mi-app este nombre 
viene con los datos que mande al collector.

java -javaagent:opentelemetry-javaagent.jar \
-Dotel.service.name=mi-app \
-Dotel.traces.exporter=otlp \
-Dotel.metrics.exporter=otlp \
-Dotel.metric.export.interval=10000 \
-Dotel.logs.exporter=otlp \
-Dotel.exporter.otlp.endpoint=http://localhost:4317 \
-Dotel.exporter.otlp.protocol=grpc \
-Dotel.instrumentation.logback-appender.enabled=true \
-Dotel.instrumentation.logback-mdc.enabled=true \
-Dotel.instrumentation.logback-appender.experimental.capture-mdc-attributes=* \
-jar ./target/app-1.0.0.jar -> Se ejecuta el mismo comando para levantar la app con openTelemetry 
	solo que se debe considerar que ahora la app manda metricas.
	- -Dotel.metric.export.interval=10000 -> Para que Cada 10 segundos mande métricas al Collector.

En el archivo de configuración del collector se agrega el exporter y el pipeline.

En el archivo de configuracion de prometheus se agrega scrapear al collector.



//////////////////////INSTRUMENTAR SPRING BOOT CON OPENTELEMETRY (PRODUCCION)///////////////////


ARQUITECTURAS PRODUCTIVAS

Esta teoría es de acuerdo a chatgpt.

Modelo: collector distribuido + grafana central ------------------

Cada EC2 Proyecto
 └─ OpenTelemetry Collector (local)
        │
        ▼
EC2 Observabilidad
 ├─ Tempo
 ├─ Prometheus
 ├─ Loki
 └─ Grafana

Aquí:
Cada proyecto tiene su Collector local
Ese Collector envía datos al backend central

Este modelo se usa mucho en empresas medianas.

Modelo: Todo separado ---------------------------------------

EC2 1 → Grafana
EC2 2 → Tempo
EC2 3 → Prometheus / Mimir
EC2 4 → Loki
EC2 5 → Collector cluster
Ec2 6 - N -> Cada app en una instancia diferente.

Este modelo es el ideal pero es el mas caro.


Modelo: Personalizado ---------------------------------------

Con base en los modelos que ha propuesto la ia se pensó un modelo mejor.

EC2 1 → Grafana
EC2 2 → Tempo
EC2 3 → Prometheus / Mimir
EC2 4 → Loki
EC2 5 → Collector cluster
Ec2 6 - N -> Cada app con su propio collector en una instancia diferente.

Este modelo es igual de caro que el ideal pero con la diferencia es que cada app tiene su 
propio collector con la finalidad de filtrar información y que al collector maestro no le 
lleguen tantos datos.


CONSTRUCCION DEL DOCKER COMPOSE

No es necesario usar depends_on hacia Grafana porque Grafana solo es la capa de visualización. Es 
decir, servicios como Prometheus, Loki o Tempo deben funcionar de manera totalmente independiente 
recolectando y almacenando métricas, logs y trazas, aunque Grafana no esté disponible.

Asi mismo, lo ideal es que todas las herramientas puedan estar levantadas de manera independiente
para que puedan seguir recolentando datos. En resumen no se debe usar depends on en ninguna 
herramienta.

ram -----------------------------------------

Considerando que la instancia donde se despliegan las herramientas tiene 8gb de ram y que todas
las herramientas viven en la misma instancia pero cada una en su propio contenedor, se le pido
a la IA hacer una comparación para ayudar a elegir la cantidad de ram limite que debe consumir 
cada contenedor. 

Herramienta 	Uso de RAM 	Cantidad limite
Prometheus 	Alto		 2.5 GB  (~31%)
Loki       	Medio Alto	 2.0 GB  (~25%)
Tempo      	Medio		 1.2 GB  (~15%)
Collector  	Bajo		 0.6 GB  (~7%)
Grafana    	Bajo		 0.5 GB  (~6%)
Libre      	--		~1.2 GB (~16%)

En general, el host siempre debe tener el 15% al 20% de memoria libre de ram. *****


CONFIGURACION DE LAS INSTANCIAS

Hay dos instancias: la instancia con la app y la instancia de observabilidad (con las herramientas: 
grafana, loki, promethues, collector y tempo).

En la instancia de observabilidad se abren las siguiente reglas de entrada:
	- puerto 4317 para que el collector reciba datos.
	- puerto 3000 para poder acceder a grafana.

Idealmente la configuración en la gui de aws debe ser la siguiente: 
Campo	Collector	Grafana
Puerto	4317		3000
Tipo	TCP		TCP
Source	instancia APP	tu IP

En la instancia de la app, Probablemente NO necesitas abrir ninguna regla de salida (outbound)
porque Por defecto, los Security Groups tienen esto: Outbound: 0.0.0.0/0 → ALLOW y Eso significa:
la instancia puede salir a cualquier IP en cualquier puerto. 

Si fuese el caso de tener que abrir una regla de salida, la configuración en aws debe ser la 
siguiente:
Campo		Valor
Type		Custom TCP
Port		4317
Destination	IP de la instancia de observabilidad

*En la instancia de la app*
nc -zv IP_INSTANCIA_OBSERVABILIDAD 4317 -> Verifica que la regla de salida en la instancia con la 
	app funcione y que la regla de entrada en la instancia de observabilidad funcione.
	Si este comando falla no se puede determinar que fallo exactamente.

Para debuggear *Dentro de la instancia de observabilidad*: docker ps y docker logs colllector

ss -tulnp | grep 4317 -> Verifica si hay un proceso escuchando en el puerto 4317 dentro de la instancia.